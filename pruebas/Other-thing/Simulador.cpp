//Elaborar un programa que simule un retiro de dinero. Debe pedir un "monto a retirar" y validar:
//Que el monto sea positivo.
//Que sea múltiplo de 10 (porque el cajero no da monedas).
//Si es válido, imprimir: "Retiro exitoso de [monto]". Si no, imprimir el error específico.
//Objetivo: Dominar el uso de if anidados y el operador módulo %.
#include <iostream>
using namespace std;
int cajero(int money)
{
    cout<<"Cuanto dinero desea retirar?"<<endl;
    cin>>money;
    return 0;
}